<% content_for :title, "Mes crédits - Repreneur - Idéal Reprise" %>
<% content_for :page_title, "Mes crédits" %>

<div class="max-w-5xl mx-auto space-y-6">
  <!-- Balance Card -->
  <div class="bg-gradient-to-r from-blue-500 to-purple-600 rounded-xl shadow-lg p-6 text-white">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-blue-100 mb-2">Solde actuel</p>
        <div class="text-5xl font-bold">45</div>
        <p class="text-blue-100 mt-2">crédits disponibles</p>
      </div>
      <%= link_to upgrade_mockups_buyer_subscription_path, class: "px-6 py-3 bg-white text-blue-600 rounded-full hover:bg-blue-50 transition font-medium shadow-lg" do %>
        Acheter des crédits
      <% end %>
    </div>
  </div>

  <!-- Stats -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div class="bg-white rounded-xl shadow-sm p-6">
      <div class="text-sm text-gray-600 mb-2">Crédits utilisés ce mois</div>
      <div class="text-2xl font-bold text-gray-900">15</div>
    </div>
    <div class="bg-white rounded-xl shadow-sm p-6">
      <div class="text-sm text-gray-600 mb-2">Économies potentielles</div>
      <div class="text-2xl font-bold text-green-600">30€</div>
    </div>
    <div class="bg-white rounded-xl shadow-sm p-6">
      <div class="text-sm text-gray-600 mb-2">Crédits totaux achetés</div>
      <div class="text-2xl font-bold text-gray-900">100</div>
    </div>
  </div>

  <!-- Transaction History -->
  <div class="bg-white rounded-xl shadow-sm p-6">
    <h3 class="text-lg font-bold text-gray-900 mb-4">Historique des transactions</h3>
    <div class="space-y-4">
      <% [
        { date: '14/10/2025', type: 'Dépense', description: 'Enrichissement - Restaurant Lyon', amount: -10, color: 'red' },
        { date: '12/10/2025', type: 'Dépense', description: 'Enrichissement - Boulangerie Paris', amount: -10, color: 'red' },
        { date: '10/10/2025', type: 'Achat', description: 'Pack 50 crédits', amount: +50, color: 'green' },
        { date: '05/10/2025', type: 'Dépense', description: 'Enrichissement - Garage Toulouse', amount: -10, color: 'red' },
        { date: '01/10/2025', type: 'Bonus', description: 'Inscription - Crédits offerts', amount: +25, color: 'blue' }
      ].each do |transaction| %>
        <div class="flex items-center justify-between p-4 border-b border-gray-100">
          <div class="flex-1">
            <div class="font-semibold text-gray-900"><%= transaction[:description] %></div>
            <div class="flex items-center gap-3 mt-1 text-sm text-gray-600">
              <span><%= transaction[:date] %></span>
              <span>• <%= transaction[:type] %></span>
            </div>
          </div>
          <div class="text-right">
            <% case transaction[:color] %>
            <% when 'red' %>
              <div class="text-lg font-bold text-red-600">
                <%= transaction[:amount] > 0 ? '+' : '' %><%= transaction[:amount] %> crédits
              </div>
            <% when 'green' %>
              <div class="text-lg font-bold text-green-600">
                <%= transaction[:amount] > 0 ? '+' : '' %><%= transaction[:amount] %> crédits
              </div>
            <% when 'blue' %>
              <div class="text-lg font-bold text-blue-600">
                <%= transaction[:amount] > 0 ? '+' : '' %><%= transaction[:amount] %> crédits
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Pricing Info -->
  <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
    <h3 class="font-semibold text-blue-900 mb-2">💰 Tarification des crédits</h3>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-3 text-sm text-blue-800">
      <div>• 10 crédits = 10€</div>
      <div>• 50 crédits = 45€ (10% de réduction)</div>
      <div>• 100 crédits = 80€ (20% de réduction)</div>
    </div>
  </div>
</div>

<% content_for :title, "Nouveau dossier - Repreneur - Idéal Reprise" %>
<% content_for :page_title, "Ajouter au pipeline" %>

<div class="max-w-4xl mx-auto space-y-6">
  <%= link_to mockups_buyer_pipeline_path, class: "inline-flex items-center gap-2 text-gray-600 hover:text-gray-900" do %>
    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
    </svg>
    Retour au pipeline
  <% end %>

  <h2 class="text-2xl font-bold text-gray-900">Ajouter une annonce au pipeline</h2>

  <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
    <p class="text-sm text-blue-900">💡 Vous pouvez ajouter n'importe quelle annonce à votre pipeline pour la suivre et organiser votre processus d'acquisition.</p>
  </div>

  <form class="space-y-6">
    <div class="bg-white rounded-xl shadow-sm p-6">
      <h3 class="text-lg font-bold text-gray-900 mb-4">Sélectionner une annonce</h3>
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Rechercher une annonce</label>
          <input type="text" placeholder="Nom de l'entreprise, ville, secteur..." class="w-full px-4 py-2 border border-gray-300 rounded-lg">
        </div>

        <!-- Search Results (mockup) -->
        <div class="border border-gray-200 rounded-lg divide-y divide-gray-200 max-h-64 overflow-y-auto">
          <% [
            { name: 'Boulangerie Paris 15ème', ca: '450k€', location: 'Paris 15ème' },
            { name: 'Restaurant Italien Lyon', ca: '420k€', location: 'Lyon 6ème' },
            { name: 'Garage Toulouse', ca: '280k€', location: 'Toulouse' }
          ].each do |listing| %>
            <label class="flex items-center gap-3 p-4 hover:bg-gray-50 cursor-pointer">
              <input type="radio" name="listing" class="border-gray-300 text-blue-600 focus:ring-blue-500">
              <div class="flex-1">
                <div class="font-semibold text-gray-900"><%= listing[:name] %></div>
                <div class="text-sm text-gray-600"><%= listing[:location] %> • CA <%= listing[:ca] %></div>
              </div>
            </label>
          <% end %>
        </div>
      </div>
    </div>

    <div class="bg-white rounded-xl shadow-sm p-6">
      <h3 class="text-lg font-bold text-gray-900 mb-4">Paramètres du dossier</h3>
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Statut initial</label>
          <select class="w-full px-4 py-2 border border-gray-300 rounded-lg">
            <option selected>À contacter</option>
            <option>En relation</option>
            <option>Étude en cours</option>
            <option>Négociation</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Note initiale (optionnel)</label>
          <textarea class="w-full px-4 py-2 border border-gray-300 rounded-lg" rows="3" placeholder="Ajoutez une note pour ce dossier..."></textarea>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Tags (optionnel)</label>
          <input type="text" placeholder="urgent, paris, commerce..." class="w-full px-4 py-2 border border-gray-300 rounded-lg">
        </div>
      </div>
    </div>

    <div class="flex justify-between">
      <%= link_to mockups_buyer_pipeline_path, class: "px-6 py-3 border border-gray-300 rounded-full text-gray-700 hover:bg-gray-50 transition font-medium" do %>
        Annuler
      <% end %>
      <button type="submit" class="px-6 py-3 bg-blue-600 text-white rounded-full hover:bg-blue-700 transition font-medium">
        Ajouter au pipeline
      </button>
    </div>
  </form>
</div>

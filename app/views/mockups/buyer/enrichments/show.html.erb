<% content_for :title, "Détail de l'enrichissement - Repreneur - Idéal Reprise" %>
<% content_for :page_title, "Détail de l'enrichissement" %>

<div class="max-w-5xl mx-auto space-y-6">
  <%= link_to mockups_buyer_enrichments_path, class: "inline-flex items-center gap-2 text-gray-600 hover:text-gray-900" do %>
    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
    </svg>
    Retour aux enrichissements
  <% end %>

  <div class="bg-white rounded-xl shadow-sm p-6">
    <div class="flex items-start justify-between mb-6">
      <div>
        <h2 class="text-2xl font-bold text-gray-900 mb-2">Boulangerie Paris 15ème</h2>
        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800">
          ✓ Complété
        </span>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
      <div class="p-4 bg-gray-50 rounded-lg">
        <div class="text-sm text-gray-600">Date de soumission</div>
        <div class="font-semibold text-gray-900">10/10/2025</div>
      </div>
      <div class="p-4 bg-gray-50 rounded-lg">
        <div class="text-sm text-gray-600">Date de completion</div>
        <div class="font-semibold text-gray-900">12/10/2025</div>
      </div>
      <div class="p-4 bg-gray-50 rounded-lg">
        <div class="text-sm text-gray-600">Coût</div>
        <div class="font-semibold text-blue-600">10 crédits</div>
      </div>
    </div>

    <div class="space-y-4">
      <h3 class="text-lg font-bold text-gray-900">Documents disponibles</h3>
      
      <% [
        { name: 'Rapport de synthèse.pdf', size: '2.4 Mo', icon: '📄' },
        { name: 'Bilan comptable 2024.pdf', size: '1.8 Mo', icon: '📊' },
        { name: 'Compte de résultat 2024.pdf', size: '1.2 Mo', icon: '📈' },
        { name: 'Analyse financière.pdf', size: '3.1 Mo', icon: '💹' }
      ].each do |doc| %>
        <div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center text-xl">
              <%= doc[:icon] %>
            </div>
            <div>
              <div class="font-semibold text-gray-900"><%= doc[:name] %></div>
              <div class="text-sm text-gray-600"><%= doc[:size] %></div>
            </div>
          </div>
          <button class="px-4 py-2 bg-blue-600 text-white rounded-full hover:bg-blue-700 transition font-medium text-sm">
            Télécharger
          </button>
        </div>
      <% end %>
    </div>
  </div>
</div>

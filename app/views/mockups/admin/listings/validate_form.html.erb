<% content_for :title, "Valider l'annonce - Administration - Idéal Reprise" %>

<div class="max-w-4xl mx-auto space-y-6">
  <%= link_to mockups_admin_listing_path(1), class: "inline-flex items-center gap-2 text-gray-600 hover:text-gray-900" do %>
    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
    </svg>
    Retour
  <% end %>

  <div>
    <h1 class="text-2xl font-bold text-gray-900">Valider l'annonce</h1>
    <p class="text-gray-600 mt-1">Boulangerie artisanale - Paris 11e</p>
  </div>

  <!-- Warning sourcing -->
  <div class="bg-yellow-50 border-2 border-yellow-400 rounded-lg p-4">
    <div class="flex gap-3">
      <svg class="w-6 h-6 text-yellow-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
      </svg>
      <div class="text-sm text-yellow-800">
        <p class="font-semibold">⚠️ Contrôle sourcing</p>
        <p class="mt-1">Vérifiez si cette annonce vient du sourcing avant de l'attribuer. L'attribution des deals sourcés sera automatisée ultérieurement.</p>
      </div>
    </div>
  </div>

  <form class="space-y-6">
    <!-- Validation basique -->
    <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
      <h3 class="text-lg font-bold text-gray-900 mb-4">Validation</h3>
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Statut</label>
          <select class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500">
            <option>Validée</option>
            <option>À modifier</option>
            <option>Rejetée</option>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Commentaires (optionnel)</label>
          <textarea rows="3" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500" placeholder="Notes internes ou feedback pour le cédant..."></textarea>
        </div>
      </div>
    </div>

    <!-- Attribuer deal exclusif (NOUVEAU) -->
    <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
      <h3 class="text-lg font-bold text-gray-900 mb-4">Attribution exclusive (optionnel)</h3>
      <p class="text-sm text-gray-600 mb-4">Vous pouvez attribuer ce deal en exclusivité à un repreneur spécifique (sourcing personnalisé)</p>
      
      <div class="space-y-4">
        <label class="flex items-center gap-3">
          <input type="checkbox" class="rounded border-gray-300 text-admin-600 focus:ring-admin-500" id="attributeDeal">
          <span class="text-sm font-medium text-gray-900">Attribuer ce deal en exclusivité</span>
        </label>

        <div id="attributionForm" class="hidden space-y-4 pl-8">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Repreneur</label>
            <select class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500">
              <option>Sélectionner un repreneur...</option>
              <option>Marie L. - Premium - Services/Digital</option>
              <option>Pierre D. - Club - Commerce</option>
              <option>Sophie M. - Premium - Restauration</option>
            </select>
          </div>
          
          <div class="bg-admin-50 border border-admin-200 rounded-lg p-3">
            <p class="text-xs text-admin-800">
              <strong>Note:</strong> Cette annonce sera visible uniquement par le repreneur sélectionné et ne sera pas publiée publiquement.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Actions -->
    <div class="flex gap-3">
      <%= link_to mockups_admin_listing_path(1), class: "px-6 py-3 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition" do %>
        Annuler
      <% end %>
      <button type="submit" class="flex-1 px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700 transition font-medium">
        Valider et publier
      </button>
    </div>
  </form>
</div>

<script>
  document.getElementById('attributeDeal')?.addEventListener('change', function() {
    document.getElementById('attributionForm').classList.toggle('hidden', !this.checked);
  });
</script>

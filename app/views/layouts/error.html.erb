<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Erreur - Idéal Reprise" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>
  <body class="bg-gray-50 font-sans antialiased">
    <!-- Simple Navigation for Error Pages -->
    <nav class="bg-white shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center" do %>
              <%= image_tag "ideal.png", class: "h-10 w-auto", alt: "Idéal Reprise" %>
            <% end %>
          </div>
          <div class="flex items-center space-x-4">
            <%= link_to "Accueil", root_path, class: "text-gray-700 hover:text-[#4A90E2] px-4 py-2 text-sm font-medium transition" %>
            <%= link_to "Se connecter", new_user_session_path, class: "text-gray-700 hover:text-[#4A90E2] px-4 py-2 text-sm font-medium transition" %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>

    <!-- Simple Footer -->
    <footer class="bg-gray-900 text-white mt-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 text-center">
        <p class="text-gray-400">&copy; <%= Time.current.year %> Idéal Reprise. Tous droits réservés.</p>
      </div>
    </footer>
  </body>
</html>

<% content_for :title, "Choisir votre abonnement - Idéal Reprise" %>

<div class="min-h-screen bg-gray-50 py-12">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">Choisissez votre abonnement</h1>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        Accédez à toutes les fonctionnalités pour réussir votre projet de 
        <%= current_user.role == 'buyer' ? 'reprise' : (current_user.role == 'seller' ? 'cession' : 'partenariat') %>
      </p>
    </div>

    <% if @user_role == 'buyer' %>
      <!-- Buyer Plans -->
      <div class="grid md:grid-cols-3 gap-8 mb-12">
        <% @buyer_plans.each do |plan| %>
          <div class="relative bg-white rounded-2xl shadow-sm <%= plan[:popular] ? 'ring-2 ring-blue-500' : 'border border-gray-200' %>">
            <% if plan[:popular] %>
              <div class="absolute -top-4 left-1/2 -translate-x-1/2">
                <span class="inline-flex items-center px-4 py-1 rounded-full text-sm font-semibold bg-blue-500 text-white">
                  ⭐ Plus populaire
                </span>
              </div>
            <% end %>
            
            <div class="p-8">
              <h3 class="text-xl font-bold text-gray-900 mb-2"><%= plan[:name] %></h3>
              <div class="flex items-baseline gap-1 mb-6">
                <span class="text-4xl font-bold text-gray-900"><%= plan[:price] %>€</span>
                <span class="text-gray-500">/<%= plan[:period] %></span>
              </div>
              
              <ul class="space-y-3 mb-8">
                <% plan[:features].each do |feature| %>
                  <li class="flex items-start gap-3">
                    <svg class="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                    </svg>
                    <span class="text-gray-600"><%= feature %></span>
                  </li>
                <% end %>
              </ul>
              
              <%= link_to checkout_payment_path(plan: plan[:id]), 
                  class: "block w-full text-center py-3 px-4 rounded-lg font-semibold transition #{plan[:popular] ? 'bg-blue-600 text-white hover:bg-blue-700' : 'bg-gray-100 text-gray-900 hover:bg-gray-200'}" do %>
                Choisir ce plan
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
      
    <% elsif @user_role == 'seller' %>
      <!-- Seller Plans -->
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto mb-12">
        <% @seller_plans.each do |plan| %>
          <div class="relative bg-white rounded-2xl shadow-sm <%= plan[:popular] ? 'ring-2 ring-blue-500' : 'border border-gray-200' %>">
            <% if plan[:popular] %>
              <div class="absolute -top-4 left-1/2 -translate-x-1/2">
                <span class="inline-flex items-center px-4 py-1 rounded-full text-sm font-semibold bg-blue-500 text-white">
                  ⭐ Recommandé
                </span>
              </div>
            <% end %>
            
            <div class="p-8">
              <h3 class="text-xl font-bold text-gray-900 mb-2"><%= plan[:name] %></h3>
              <div class="flex items-baseline gap-1 mb-6">
                <% if plan[:price] == 0 %>
                  <span class="text-4xl font-bold text-gray-900">Gratuit</span>
                <% else %>
                  <span class="text-4xl font-bold text-gray-900"><%= plan[:price] %>€</span>
                  <span class="text-gray-500">/<%= plan[:period] %></span>
                <% end %>
              </div>
              
              <ul class="space-y-3 mb-8">
                <% plan[:features].each do |feature| %>
                  <li class="flex items-start gap-3">
                    <svg class="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                    </svg>
                    <span class="text-gray-600"><%= feature %></span>
                  </li>
                <% end %>
              </ul>
              
              <% if plan[:price] == 0 %>
                <span class="block w-full text-center py-3 px-4 rounded-lg font-semibold bg-gray-100 text-gray-500 cursor-default">
                  Plan actuel
                </span>
              <% else %>
                <%= link_to checkout_payment_path(plan: plan[:id]), 
                    class: "block w-full text-center py-3 px-4 rounded-lg font-semibold bg-blue-600 text-white hover:bg-blue-700 transition" do %>
                  Passer Premium
                <% end %>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
      
    <% elsif @user_role == 'partner' %>
      <!-- Partner Plans -->
      <div class="max-w-lg mx-auto mb-12">
        <% @partner_plans.each do |plan| %>
          <div class="bg-white rounded-2xl shadow-sm ring-2 ring-teal-500 p-8">
            <h3 class="text-xl font-bold text-gray-900 mb-2"><%= plan[:name] %></h3>
            <div class="flex items-baseline gap-1 mb-6">
              <span class="text-4xl font-bold text-gray-900"><%= plan[:price] %>€</span>
              <span class="text-gray-500">/<%= plan[:period] %></span>
            </div>
            
            <ul class="space-y-3 mb-8">
              <% plan[:features].each do |feature| %>
                <li class="flex items-start gap-3">
                  <svg class="w-5 h-5 text-green-500 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                  <span class="text-gray-600"><%= feature %></span>
                </li>
              <% end %>
            </ul>
            
            <%= link_to checkout_payment_path(plan: plan[:id]), 
                class: "block w-full text-center py-3 px-4 rounded-lg font-semibold bg-teal-600 text-white hover:bg-teal-700 transition" do %>
              S'inscrire à l'annuaire
            <% end %>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Credit Packs Section -->
    <% if @user_role.in?(['buyer', 'seller']) %>
      <div class="border-t border-gray-200 pt-12">
        <h2 class="text-2xl font-bold text-gray-900 text-center mb-8">Acheter des crédits</h2>
        <div class="grid sm:grid-cols-3 gap-6 max-w-3xl mx-auto">
          <% @credit_packs.each do |pack| %>
            <div class="bg-white rounded-xl border border-gray-200 p-6 text-center hover:border-blue-300 transition">
              <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              </div>
              <h3 class="text-lg font-bold text-gray-900 mb-1"><%= pack[:name] %></h3>
              <div class="text-2xl font-bold text-gray-900 mb-1"><%= pack[:price] %>€</div>
              <% if pack[:discount] %>
                <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-700 mb-4">
                  -<%= pack[:discount] %>
                </span>
              <% else %>
                <div class="h-6 mb-4"></div>
              <% end %>
              <%= link_to checkout_payment_path(plan: pack[:id]), 
                  class: "block w-full text-center py-2 px-4 rounded-lg font-medium bg-gray-100 text-gray-900 hover:bg-gray-200 transition" do %>
                Acheter
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <!-- FAQ Section -->
    <div class="mt-16 max-w-3xl mx-auto">
      <h2 class="text-2xl font-bold text-gray-900 text-center mb-8">Questions fréquentes</h2>
      <div class="space-y-4">
        <details class="bg-white rounded-lg border border-gray-200 p-4 group">
          <summary class="font-semibold text-gray-900 cursor-pointer flex items-center justify-between">
            Comment fonctionne la facturation ?
            <svg class="w-5 h-5 text-gray-500 group-open:rotate-180 transition" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
            </svg>
          </summary>
          <p class="mt-3 text-gray-600">
            Vous êtes facturé au début de chaque période (mensuelle ou annuelle). 
            Vous pouvez annuler à tout moment, votre accès reste actif jusqu'à la fin de la période en cours.
          </p>
        </details>
        
        <details class="bg-white rounded-lg border border-gray-200 p-4 group">
          <summary class="font-semibold text-gray-900 cursor-pointer flex items-center justify-between">
            Puis-je changer de plan ?
            <svg class="w-5 h-5 text-gray-500 group-open:rotate-180 transition" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
            </svg>
          </summary>
          <p class="mt-3 text-gray-600">
            Oui, vous pouvez passer à un plan supérieur à tout moment. Le prorata sera calculé automatiquement.
            Le passage à un plan inférieur prendra effet à la prochaine période de facturation.
          </p>
        </details>
        
        <details class="bg-white rounded-lg border border-gray-200 p-4 group">
          <summary class="font-semibold text-gray-900 cursor-pointer flex items-center justify-between">
            Quels moyens de paiement acceptez-vous ?
            <svg class="w-5 h-5 text-gray-500 group-open:rotate-180 transition" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
            </svg>
          </summary>
          <p class="mt-3 text-gray-600">
            Nous acceptons les cartes bancaires (Visa, Mastercard, American Express) et les virements SEPA 
            pour les abonnements annuels. Tous les paiements sont sécurisés via Stripe.
          </p>
        </details>
      </div>
    </div>

    <!-- Contact CTA -->
    <div class="mt-12 text-center">
      <p class="text-gray-600 mb-4">Besoin d'aide pour choisir ?</p>
      <%= link_to contact_path, class: "inline-flex items-center gap-2 text-blue-600 hover:text-blue-700 font-medium" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
        </svg>
        Contactez notre équipe
      <% end %>
    </div>
  </div>
</div>

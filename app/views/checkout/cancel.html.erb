<% content_for :title, "Paiement annulé - Idéal Reprise" %>

<div class="min-h-screen bg-gray-50 flex items-center justify-center px-4 py-16">
  <div class="max-w-lg w-full text-center">
    <!-- Cancel Illustration -->
    <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-8">
      <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </div>

    <!-- Message -->
    <h1 class="text-3xl font-bold text-gray-900 mb-4">Paiement non finalisé</h1>
    <p class="text-gray-600 mb-8">
      <% case @reason
         when 'declined' %>
        Votre carte a été refusée. Veuillez vérifier vos informations ou utiliser une autre carte.
      <% when 'expired' %>
        La session de paiement a expiré. Veuillez réessayer.
      <% when 'error' %>
        Une erreur technique est survenue. Notre équipe a été notifiée.
      <% else %>
        Votre paiement a été annulé. Aucun montant n'a été débité.
      <% end %>
    </p>

    <!-- Help Card -->
    <div class="bg-white rounded-xl shadow-sm p-6 mb-8 text-left">
      <h3 class="font-semibold text-gray-900 mb-3 flex items-center gap-2">
        <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        Que faire maintenant ?
      </h3>
      <ul class="space-y-2 text-sm text-gray-600">
        <li class="flex items-start gap-2">
          <span class="text-gray-400">•</span>
          Vérifiez que votre carte est valide et dispose de fonds suffisants
        </li>
        <li class="flex items-start gap-2">
          <span class="text-gray-400">•</span>
          Essayez avec une autre carte bancaire
        </li>
        <li class="flex items-start gap-2">
          <span class="text-gray-400">•</span>
          Contactez votre banque si le problème persiste
        </li>
        <li class="flex items-start gap-2">
          <span class="text-gray-400">•</span>
          Notre support est disponible pour vous aider
        </li>
      </ul>
    </div>

    <!-- Action Buttons -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <%= link_to checkout_select_plan_path, class: "inline-flex items-center gap-2 px-6 py-3 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
        </svg>
        Réessayer
      <% end %>
      
      <%= link_to root_path, class: "inline-flex items-center gap-2 px-6 py-3 border-2 border-gray-300 text-gray-700 rounded-lg font-semibold hover:bg-gray-50 transition" do %>
        Retour à l'accueil
      <% end %>
    </div>

    <!-- Contact Support -->
    <div class="mt-8 p-4 bg-amber-50 rounded-lg">
      <p class="text-sm text-amber-800 flex items-center justify-center gap-2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192l-3.536 3.536M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-5 0a4 4 0 11-8 0 4 4 0 018 0z"/>
        </svg>
        Besoin d'aide ? 
        <%= link_to "Contactez notre support", contact_path, class: "font-semibold underline" %>
      </p>
    </div>
  </div>
</div>

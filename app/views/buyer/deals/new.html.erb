<% content_for :title, "Nouveau dossier - Idéal Reprise" %>
<% content_for :page_title, "Ajouter un dossier au pipeline" %>

<div class="max-w-3xl mx-auto space-y-6">
  <!-- Back Navigation -->
  <div>
    <%= link_to buyer_pipeline_path, class: "inline-flex items-center text-sm text-buyer-600 hover:text-buyer-700" do %>
      <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
      </svg>
      Retour au pipeline
    <% end %>
  </div>

  <!-- Info Banner -->
  <div class="bg-buyer-50 border border-buyer-200 rounded-lg p-4">
    <div class="flex items-start gap-3">
      <svg class="w-5 h-5 text-buyer-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <div class="text-sm text-buyer-700">
        <p class="font-medium mb-1">À propos de l'ajout manuel</p>
        <p>Les dossiers sont généralement créés automatiquement lorsque vous ajoutez une annonce à vos favoris. Utilisez cette fonctionnalité uniquement si vous devez ajouter manuellement un dossier existant.</p>
      </div>
    </div>
  </div>

  <!-- New Deal Form -->
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
    <h3 class="text-lg font-bold text-gray-900 mb-6">Nouveau dossier</h3>

    <%= form_with model: @deal, url: buyer_deals_path, method: :post, local: true, class: "space-y-6" do |f| %>
      <!-- Error Messages -->
      <% if @deal.errors.any? %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4">
          <div class="flex">
            <svg class="h-5 w-5 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-red-800">
                <%= pluralize(@deal.errors.count, "erreur") %> détectée<%= 's' if @deal.errors.count > 1 %>
              </h3>
              <ul class="mt-2 text-sm text-red-700 list-disc list-inside">
                <% @deal.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Listing Selection -->
      <div>
        <%= f.label :listing_id, "Annonce", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.collection_select :listing_id, 
          Listing.approved.order(:title), 
          :id, 
          :title,
          { prompt: "Sélectionnez une annonce" },
          class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-buyer-500 focus:border-buyer-500 transition" %>
        <p class="mt-2 text-sm text-gray-500">
          Sélectionnez l'annonce que vous souhaitez ajouter à votre pipeline CRM.
        </p>
      </div>

      <!-- Initial Notes -->
      <div>
        <%= f.label :notes, "Notes initiales (optionnel)", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_area :notes, 
          rows: 6,
          placeholder: "Ajoutez vos premières impressions, objectifs ou points à vérifier...",
          class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-buyer-500 focus:border-buyer-500 transition" %>
        <p class="mt-2 text-sm text-gray-500">
          Vous pourrez modifier et compléter ces notes à tout moment depuis la page du dossier.
        </p>
      </div>

      <!-- Initial Stage Info -->
      <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
        <div class="flex items-start gap-3">
          <div class="flex-shrink-0">
            <span class="inline-flex items-center justify-center w-8 h-8 bg-gray-100 rounded-full">
              ❤️
            </span>
          </div>
          <div>
            <h4 class="text-sm font-semibold text-gray-900 mb-1">Étape initiale : Favoris</h4>
            <p class="text-sm text-gray-600">
              Le dossier sera automatiquement ajouté dans la colonne "Favoris" de votre pipeline. Vous pourrez ensuite le déplacer vers les étapes suivantes au fur et à mesure de votre avancement.
            </p>
          </div>
        </div>
      </div>

      <!-- Actions -->
      <div class="flex items-center justify-between pt-4 border-t border-gray-200">
        <%= link_to "Annuler", buyer_pipeline_path, 
          class: "px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition font-medium" %>
        
        <%= f.submit "Ajouter au pipeline", 
          class: "px-6 py-2 bg-buyer-600 text-white rounded-lg hover:bg-buyer-700 transition font-medium shadow-sm" %>
      </div>
    <% end %>
  </div>

  <!-- Alternative Method -->
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
    <h3 class="text-sm font-semibold text-gray-900 mb-3">💡 Méthode recommandée</h3>
    <p class="text-sm text-gray-600 mb-4">
      Plutôt que d'ajouter manuellement un dossier, nous vous recommandons d'utiliser le bouton "Ajouter aux favoris" directement depuis la page d'une annonce. Cette méthode est plus simple et crée automatiquement le dossier dans votre pipeline.
    </p>
    <%= link_to buyer_listings_path, class: "inline-flex items-center text-sm text-buyer-600 hover:text-buyer-700 font-medium" do %>
      Parcourir les annonces
      <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
      </svg>
    <% end %>
  </div>
</div>

<% content_for :title, "Tableau de bord Repreneur - Idéal Reprise" %>

<div class="space-y-6">
  <!-- Welcome Banner with Scrolling Messages -->
  <div class="bg-gradient-to-r from-buyer-500 to-buyer-600 rounded-xl shadow-sm p-6 text-white">
    <div class="flex items-center justify-between mb-3">
      <div>
        <h2 class="text-2xl font-bold mb-2">Bonjour <%= current_user.full_name&.split(' ')&.first || 'Repreneur' %> 👋</h2>
        <p class="text-buyer-100">Bienvenue sur votre espace repreneur</p>
      </div>
      <div class="hidden md:block">
        <%= link_to buyer_listings_path, class: "inline-flex items-center gap-2 px-6 py-3 bg-white text-buyer-600 rounded-full font-medium hover:bg-buyer-50 transition shadow-lg" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          Parcourir les annonces
        <% end %>
      </div>
    </div>
    <!-- Auto-Rotating Messages Carousel -->
    <div class="bg-white/10 rounded-lg py-2 px-3" data-controller="carousel">
      <div class="flex items-center gap-2">
        <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <div class="flex-1 relative overflow-hidden" style="min-height: 1.5rem;">
          <!-- Slide 1 -->
          <div data-carousel-target="slide" class="transition-opacity duration-300 ease-in-out absolute inset-0">
            <p class="text-sm">
              📢 Nouveauté : Complétez votre profil repreneur pour recevoir des propositions ciblées
            </p>
          </div>
          <!-- Slide 2 -->
          <div data-carousel-target="slide" class="transition-opacity duration-300 ease-in-out absolute inset-0 hidden opacity-0">
            <p class="text-sm">
              💡 Astuce : Libérez vos réservations non abouties pour gagner des crédits
            </p>
          </div>
          <!-- Slide 3 -->
          <div data-carousel-target="slide" class="transition-opacity duration-300 ease-in-out absolute inset-0 hidden opacity-0">
            <p class="text-sm">
              🎯 Promo : -20% sur l'abonnement Premium jusqu'à la fin du mois
            </p>
          </div>
        </div>
        <!-- Carousel Dots -->
        <div class="flex items-center gap-1.5 ml-2">
          <button type="button" data-carousel-dot data-index="0" data-action="click->carousel#goToSlide" class="w-1.5 h-1.5 rounded-full bg-white transition-all"></button>
          <button type="button" data-carousel-dot data-index="1" data-action="click->carousel#goToSlide" class="w-1.5 h-1.5 rounded-full bg-white/50 transition-all"></button>
          <button type="button" data-carousel-dot data-index="2" data-action="click->carousel#goToSlide" class="w-1.5 h-1.5 rounded-full bg-white/50 transition-all"></button>
        </div>
      </div>
    </div>
  </div>

  <!-- Expiring Timers Alert -->
  <% if @expiring_soon.any? %>
    <div class="bg-orange-50 border-2 border-orange-200 rounded-xl p-6">
      <div class="flex items-start gap-4">
        <svg class="w-6 h-6 text-orange-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <div class="flex-1">
          <h3 class="text-lg font-bold text-orange-900 mb-2">⏰ Délais qui expirent bientôt!</h3>
          <p class="text-orange-800 mb-3">
            <%= @expiring_soon.count %> réservation<%= @expiring_soon.count > 1 ? 's' : '' %> expire<%= @expiring_soon.count > 1 ? 'nt' : '' %> dans moins de 24 heures.
          </p>
          <%= link_to buyer_pipeline_path, class: "inline-flex items-center px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition font-medium" do %>
            Voir mon pipeline →
          <% end %>
        </div>
      </div>
    </div>
  <% end %>

  <!-- 4 Stats Cards - Matching Mockup Order -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
    <!-- Messages -->
    <%= link_to buyer_conversations_path, class: "bg-white rounded-xl shadow-sm p-6 border-2 border-transparent hover:border-buyer-200 transition cursor-pointer" do %>
      <div class="flex items-center justify-between mb-2">
        <div class="text-sm text-gray-600">Messages</div>
        <div class="w-10 h-10 bg-buyer-100 rounded-full flex items-center justify-center">
          <svg class="w-5 h-5 text-buyer-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
        </div>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:unread_messages] || 0 %></div>
      <div class="text-xs text-gray-500 mt-1">Non lus</div>
    <% end %>

    <!-- Réservations actives (timer le plus court) -->
    <%= link_to buyer_deals_path, class: "bg-white rounded-xl shadow-sm p-6 border-2 border-transparent hover:border-orange-200 transition cursor-pointer" do %>
      <div class="flex items-center justify-between mb-2">
        <div class="text-sm text-gray-600">Réservations actives</div>
        <div class="w-10 h-10 bg-orange-100 rounded-full flex items-center justify-center">
          <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:active_reservations] %></div>
      <% if @shortest_timer_days %>
        <div class="text-xs text-orange-600 mt-1 flex items-center gap-1">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          Plus court: <%= @shortest_timer_days %>j restants
        </div>
      <% else %>
        <div class="text-xs text-gray-500 mt-1">Réservations actives</div>
      <% end %>
    <% end %>

    <!-- Crédits -->
    <%= link_to buyer_credits_path, class: "bg-white rounded-xl shadow-sm p-6 border-2 border-transparent hover:border-purple-200 transition cursor-pointer" do %>
      <div class="flex items-center justify-between mb-2">
        <div class="text-sm text-gray-600">Crédits</div>
        <div class="w-10 h-10 bg-purple-100 rounded-full flex items-center justify-center">
          <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"/>
          </svg>
        </div>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:credits] %></div>
      <div class="text-xs text-gray-500 mt-1">Disponibles</div>
    <% end %>

    <!-- Favoris -->
    <%= link_to buyer_favorites_path, class: "bg-white rounded-xl shadow-sm p-6 border-2 border-transparent hover:border-green-200 transition cursor-pointer" do %>
      <div class="flex items-center justify-between mb-2">
        <div class="text-sm text-gray-600">Mes Favoris</div>
        <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center">
          <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
          </svg>
        </div>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:favorites] %></div>
      <% if @new_favorites_count && @new_favorites_count > 0 %>
        <div class="text-xs text-green-600 mt-1"><%= @new_favorites_count %> nouvelle<%= @new_favorites_count > 1 ? 's' : '' %> annonce<%= @new_favorites_count > 1 ? 's' : '' %></div>
      <% else %>
        <div class="text-xs text-gray-500 mt-1">Annonces sauvegardées</div>
      <% end %>
    <% end %>
  </div>

  <!-- Mon Pipeline -->
  <div class="bg-white rounded-xl shadow-sm border border-gray-200">
    <div class="p-6 border-b border-gray-200">
      <div class="flex items-center justify-between">
        <h2 class="text-lg font-bold text-gray-900">Mon Pipeline</h2>
        <%= link_to buyer_pipeline_path, class: "text-sm text-buyer-600 hover:text-buyer-700 font-medium" do %>
          Vue Kanban →
        <% end %>
      </div>
    </div>
    
    <!-- Pipeline Progress Bar -->
    <div class="p-6">
      <div class="grid grid-cols-5 sm:grid-cols-10 gap-2 mb-6">
        <% @stages.each do |stage| %>
          <div class="text-center">
            <div class="w-full h-12 bg-<%= stage[:color] %>-100 rounded-lg flex items-center justify-center mb-1 cursor-pointer hover:bg-<%= stage[:color] %>-200 transition">
              <span class="text-lg font-bold text-<%= stage[:color] %>-700"><%= stage[:count] %></span>
            </div>
            <span class="text-xs text-gray-600"><%= stage[:short_label] || stage[:label].truncate(8) %></span>
          </div>
        <% end %>
      </div>

      <p class="text-sm text-gray-600 text-center">
        <%= link_to buyer_pipeline_path, class: "text-buyer-600 hover:text-buyer-700 font-medium" do %>
          Voir le pipeline complet →
        <% end %>
      </p>
    </div>
  </div>

  <!-- Actions Rapides -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <%= link_to buyer_listings_path, class: "bg-white rounded-xl shadow-sm p-6 border border-gray-200 hover:shadow-md transition group" do %>
      <div class="flex items-center gap-4">
        <div class="w-12 h-12 bg-buyer-100 rounded-lg flex items-center justify-center group-hover:bg-buyer-200 transition">
          <svg class="w-6 h-6 text-buyer-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </div>
        <div>
          <div class="font-semibold text-gray-900 group-hover:text-buyer-600 transition">Rechercher des deals</div>
          <div class="text-sm text-gray-500"><%= @available_listings_count || 0 %> annonces disponibles</div>
        </div>
      </div>
    <% end %>

    <%= link_to partners_path, class: "bg-white rounded-xl shadow-sm p-6 border border-gray-200 hover:shadow-md transition group" do %>
      <div class="flex items-center gap-4">
        <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center group-hover:bg-purple-200 transition">
          <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
        </div>
        <div>
          <div class="font-semibold text-gray-900 group-hover:text-purple-600 transition">Trouver des partenaires</div>
          <div class="text-sm text-gray-500">Annuaire experts</div>
        </div>
      </div>
    <% end %>

    <%= link_to upgrade_buyer_subscription_path, class: "bg-gradient-to-r from-yellow-400 to-orange-500 rounded-xl shadow-sm p-6 hover:shadow-md transition group" do %>
      <div class="flex items-center gap-4">
        <div class="w-12 h-12 bg-white/30 rounded-lg flex items-center justify-center group-hover:bg-white/40 transition">
          <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
          </svg>
        </div>
        <div class="text-white">
          <div class="font-semibold text-lg">Passer Premium</div>
          <div class="text-sm text-white/80">Accès illimité</div>
        </div>
      </div>
    <% end %>
  </div>

  <!-- Recent Activity & Tips/Subscription -->
  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Activity Feed -->
    <div class="lg:col-span-2 bg-white rounded-xl shadow-sm border border-gray-200">
      <div class="p-6 border-b border-gray-200">
        <h2 class="text-lg font-bold text-gray-900">Activité récente</h2>
      </div>
      <% if @recent_activity.any? %>
        <div class="divide-y divide-gray-200">
          <% @recent_activity.each do |deal| %>
            <div class="p-4 hover:bg-gray-50 transition">
              <div class="flex gap-3">
                <div class="w-2 h-2 bg-buyer-600 rounded-full mt-2"></div>
                <div class="flex-1">
                  <%= link_to buyer_deal_path(deal), class: "block" do %>
                    <p class="text-sm text-gray-900 font-medium"><%= deal.listing.title %></p>
                    <p class="text-xs text-gray-500">Déplacé vers "<%= deal.status.humanize %>" - <%= time_ago_in_words(deal.updated_at) %></p>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="p-8 text-center">
          <svg class="w-12 h-12 text-gray-300 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <p class="text-gray-500">Aucune activité récente</p>
          <p class="text-sm text-gray-400 mt-1">Vos actions apparaîtront ici</p>
        </div>
      <% end %>
    </div>

    <!-- Tips & Subscription -->
    <div class="space-y-6">
      <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center">
            <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"/>
            </svg>
          </div>
          <div>
            <div class="font-semibold text-gray-900"><%= @subscription_name || 'Freemium' %></div>
            <div class="text-xs text-gray-500"><%= @subscription_end_date ? "Actif jusqu'au #{@subscription_end_date}" : 'Plan gratuit' %></div>
          </div>
        </div>
        <div class="text-sm text-gray-600 mb-4">
          <div class="flex justify-between mb-2">
            <span>Réservations</span>
            <span class="font-medium"><%= @stats[:active_reservations] %> / <%= @max_reservations || 'illimité' %></span>
          </div>
          <div class="flex justify-between">
            <span>Crédits</span>
            <span class="font-medium"><%= @stats[:credits] %></span>
          </div>
        </div>
        <%= link_to buyer_subscription_path, class: "block text-center text-sm text-buyer-600 hover:text-buyer-700 font-medium" do %>
          Gérer mon abonnement →
        <% end %>
      </div>

      <div class="bg-gradient-to-br from-buyer-50 to-purple-50 rounded-xl p-6 border border-buyer-200">
        <div class="flex items-start gap-3">
          <div class="w-10 h-10 bg-buyer-100 rounded-full flex items-center justify-center flex-shrink-0">
            <svg class="w-5 h-5 text-buyer-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
            </svg>
          </div>
          <div>
            <h4 class="text-sm font-semibold text-gray-900 mb-1">💡 Conseil du jour</h4>
            <p class="text-xs text-gray-700">Complétez votre profil repreneur pour recevoir des propositions ciblées des cédants !</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Favorites Widget -->
  <div class="bg-white rounded-lg shadow p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Favoris récents</h2>
      <%= link_to "Voir tout", buyer_favorites_path, class: "text-buyer-600 hover:text-buyer-700 text-sm font-medium" %>
    </div>
    <% if @recent_favorites.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <% @recent_favorites.each do |favorite| %>
          <%= render 'buyer/listings/listing_card', listing: favorite.listing, is_favorited: true %>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <svg class="w-12 h-12 text-gray-300 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
        <p class="text-gray-500">Aucun favori pour le moment</p>
        <p class="text-sm text-gray-400 mt-1">Ajoutez des annonces à vos favoris</p>
      </div>
    <% end %>
  </div>
</div>

<% content_for :title, "Tableau de bord Acheteur - Idéal Reprise" %>

<div class="space-y-6">
  <!-- Welcome Banner with Scrolling Messages -->
  <div class="bg-gradient-to-r from-buyer-500 to-buyer-600 rounded-xl shadow-sm p-6 text-white">
    <div class="flex items-center justify-between mb-3">
      <div>
        <h2 class="text-2xl font-bold mb-2">Bonjour <%= current_user.full_name&.split(' ')&.first || 'Repreneur' %> 👋</h2>
        <p class="text-buyer-100">Bienvenue sur votre espace repreneur</p>
      </div>
      <div class="hidden md:block">
        <%= link_to buyer_listings_path, class: "inline-flex items-center gap-2 px-6 py-3 bg-white text-buyer-600 rounded-full font-medium hover:bg-buyer-50 transition shadow-lg" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          Parcourir les annonces
        <% end %>
      </div>
    </div>
  </div>

  <!-- Expiring Timers Alert -->
  <% if @expiring_soon.any? %>
    <div class="bg-orange-50 border-2 border-orange-200 rounded-xl p-6">
      <div class="flex items-start gap-4">
        <svg class="w-6 h-6 text-orange-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <div class="flex-1">
          <h3 class="text-lg font-bold text-orange-900 mb-2">⏰ Délais qui expirent bientôt!</h3>
          <p class="text-orange-800 mb-3">
            <%= @expiring_soon.count %> réservation<%= @expiring_soon.count > 1 ? 's' : '' %> expire<%= @expiring_soon.count > 1 ? 'nt' : '' %> dans moins de 24 heures.
          </p>
          <%= link_to buyer_pipeline_path, class: "inline-flex items-center px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition font-medium" do %>
            Voir mon pipeline →
          <% end %>
        </div>
      </div>
    </div>
  <% end %>

  <!-- Stats Grid -->
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
    <!-- Active Deals -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex items-center justify-between mb-4">
        <div class="p-3 bg-blue-100 rounded-lg">
          <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
        </div>
        <span class="text-sm font-medium text-gray-500">Mes dossiers</span>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:total_deals] %></div>
      <p class="text-sm text-gray-500 mt-2">Dossiers actifs</p>
    </div>

    <!-- Active Reservations -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex items-center justify-between mb-4">
        <div class="p-3 bg-green-100 rounded-lg">
          <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
          </svg>
        </div>
        <span class="text-sm font-medium text-gray-500">Réservations</span>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:active_reservations] %></div>
      <p class="text-sm text-gray-500 mt-2">Réservations actives</p>
    </div>

    <!-- Favorites -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex items-center justify-between mb-4">
        <div class="p-3 bg-yellow-100 rounded-lg">
          <svg class="w-6 h-6 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z" />
          </svg>
        </div>
        <span class="text-sm font-medium text-gray-500">Favoris</span>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:favorites] %></div>
      <p class="text-sm text-gray-500 mt-2">Annonces favorites</p>
    </div>

    <!-- Credits -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex items-center justify-between mb-4">
        <div class="p-3 bg-purple-100 rounded-lg">
          <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <span class="text-sm font-medium text-gray-500">Crédits</span>
      </div>
      <div class="text-3xl font-bold text-gray-900"><%= @stats[:credits] %></div>
      <p class="text-sm text-gray-500 mt-2">Crédits disponibles</p>
    </div>
  </div>

  <!-- Quick Actions -->
  <div class="bg-white rounded-lg shadow p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">Actions rapides</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <%= link_to buyer_listings_path, class: "flex items-center gap-3 p-4 border border-gray-200 rounded-lg hover:border-buyer-500 hover:bg-buyer-50 transition" do %>
        <svg class="w-8 h-8 text-buyer-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
        <div>
          <div class="font-medium text-gray-900">Parcourir les annonces</div>
          <div class="text-sm text-gray-500">Voir toutes les opportunités</div>
        </div>
      <% end %>

      <%= link_to buyer_pipeline_path, class: "flex items-center gap-3 p-4 border border-gray-200 rounded-lg hover:border-buyer-500 hover:bg-buyer-50 transition" do %>
        <svg class="w-8 h-8 text-buyer-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
        </svg>
        <div>
          <div class="font-medium text-gray-900">Mon pipeline CRM</div>
          <div class="text-sm text-gray-500">Gérer mes dossiers</div>
        </div>
      <% end %>

      <%= link_to buyer_favorites_path, class: "flex items-center gap-3 p-4 border border-gray-200 rounded-lg hover:border-buyer-500 hover:bg-buyer-50 transition" do %>
        <svg class="w-8 h-8 text-buyer-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
        <div>
          <div class="font-medium text-gray-900">Mes favoris</div>
          <div class="text-sm text-gray-500">Annonces sauvegardées</div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <!-- Pipeline Overview Widget -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-lg font-semibold text-gray-900">Aperçu du pipeline</h2>
        <%= link_to "Voir le pipeline", buyer_pipeline_path, class: "text-buyer-600 hover:text-buyer-700 text-sm font-medium" %>
      </div>
      <% if @stages.any? { |stage| stage[:count] > 0 } %>
        <div class="space-y-3">
          <% @stages.each do |stage| %>
            <% if stage[:count] > 0 %>
              <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg hover:bg-gray-100 transition">
                <div class="flex items-center gap-3">
                  <div class="w-2 h-2 rounded-full bg-buyer-500"></div>
                  <span class="font-medium text-gray-900"><%= stage[:label] %></span>
                </div>
                <span class="px-3 py-1 bg-buyer-100 text-buyer-700 rounded-full text-sm font-medium">
                  <%= stage[:count] %>
                </span>
              </div>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-8">
          <svg class="w-12 h-12 text-gray-300 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
          </svg>
          <p class="text-gray-500">Aucun dossier en cours</p>
          <p class="text-sm text-gray-400 mt-1">Parcourez les annonces pour démarrer</p>
        </div>
      <% end %>
    </div>

    <!-- Active Reservations Widget -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-lg font-semibold text-gray-900">Réservations actives</h2>
        <%= link_to "Tout voir", buyer_deals_path, class: "text-buyer-600 hover:text-buyer-700 text-sm font-medium" %>
      </div>
      <% if @active_reservations.any? %>
        <div class="space-y-3">
          <% @active_reservations.limit(3).each do |deal| %>
            <div class="p-4 border border-gray-200 rounded-lg hover:border-buyer-300 transition">
              <%= link_to buyer_deal_path(deal), class: "block" do %>
                <div class="flex items-start justify-between mb-2">
                  <h3 class="font-medium text-gray-900"><%= deal.listing.title %></h3>
                  <% if deal.expiring_soon? %>
                    <span class="px-2 py-1 bg-red-100 text-red-700 text-xs font-medium rounded">Urgent</span>
                  <% end %>
                </div>
                <p class="text-sm text-gray-600 mb-3">
                  <%= deal.listing.location_city || deal.listing.location_department || 'France' %>
                </p>
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-500">⏱ <%= deal.days_remaining %> jours restants</span>
                  <div class="flex-1 ml-4">
                    <%= render 'buyer/deals/timer_gauge', deal: deal, compact: true %>
                  </div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-8">
          <svg class="w-12 h-12 text-gray-300 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
          </svg>
          <p class="text-gray-500">Aucune réservation active</p>
          <p class="text-sm text-gray-400 mt-1">Réservez une annonce pour débuter</p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Favorites Widget -->
  <div class="bg-white rounded-lg shadow p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Favoris récents</h2>
      <%= link_to "Voir tout", buyer_favorites_path, class: "text-buyer-600 hover:text-buyer-700 text-sm font-medium" %>
    </div>
    <% if @recent_favorites.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <% @recent_favorites.each do |favorite| %>
          <%= render 'buyer/listings/listing_card', listing: favorite.listing, is_favorited: true %>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <svg class="w-12 h-12 text-gray-300 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
        <p class="text-gray-500">Aucun favori pour le moment</p>
        <p class="text-sm text-gray-400 mt-1">Ajoutez des annonces à vos favoris</p>
      </div>
    <% end %>
  </div>

  <!-- Recent Activity Timeline -->
  <div class="bg-white rounded-lg shadow p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">Activité récente</h2>
    <% if @recent_activity.any? %>
      <div class="space-y-4">
        <% @recent_activity.each do |deal| %>
          <div class="flex gap-4">
            <div class="flex-shrink-0">
              <div class="w-10 h-10 bg-buyer-100 rounded-full flex items-center justify-center">
                <svg class="w-5 h-5 text-buyer-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                </svg>
              </div>
            </div>
            <div class="flex-1">
              <%= link_to buyer_deal_path(deal), class: "block hover:bg-gray-50 -m-2 p-2 rounded-lg transition" do %>
                <p class="font-medium text-gray-900"><%= deal.listing.title %></p>
                <p class="text-sm text-gray-600 mt-1">
                  Déplacé vers "<%= deal.status.humanize %>"
                </p>
                <p class="text-xs text-gray-400 mt-1">
                  <%= time_ago_in_words(deal.updated_at) %>
                </p>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <svg class="w-12 h-12 text-gray-300 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <p class="text-gray-500">Aucune activité récente</p>
        <p class="text-sm text-gray-400 mt-1">Vos actions apparaîtront ici</p>
      </div>
    <% end %>
  </div>
</div>

<% content_for :title, "Changer de forfait - Idéal Reprise" %>
<% content_for :page_title, "Changer votre forfait" %>

<div class="max-w-7xl mx-auto space-y-8">
  <%= link_to buyer_subscription_path, class: "inline-flex items-center gap-2 text-gray-600 hover:text-gray-900" do %>
    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
    </svg>
    Retour à mon abonnement
  <% end %>

  <% if @current_subscription %>
    <div class="text-center mb-8">
      <h2 class="text-3xl font-bold text-gray-900 mb-2">Changer votre forfait</h2>
      <p class="text-gray-600">Actuellement sur <span class="font-semibold text-buyer-600"><%= @current_subscription.plan_type.humanize.gsub('Buyer ', '') %></span></p>
    </div>

    <!-- Plan Comparison -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      <% @all_plans.each do |plan_key, plan| %>
        <% is_current = @current_plan_type == "buyer_#{plan_key}" %>
        
        <% if plan_key == :premium %>
          <!-- Premium Plan (Highlighted) -->
          <div class="relative bg-gradient-to-br from-buyer-500 to-purple-600 rounded-xl shadow-xl p-6 text-white border-2 border-buyer-600">
            <% if is_current %>
              <div class="absolute top-4 right-4 bg-white text-buyer-600 px-3 py-1 text-xs font-bold uppercase rounded-full">
                Actuel
              </div>
            <% else %>
              <div class="absolute top-4 right-4">
                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-yellow-400 text-gray-900">
                  ⭐ Recommandé
                </span>
              </div>
            <% end %>

            <div class="mb-6 <%= is_current ? '' : 'mt-8' %>">
              <h3 class="text-2xl font-bold mb-2"><%= plan[:name] %></h3>
              <div class="text-3xl font-bold mb-1"><%= plan[:price_display] %></div>
              <div class="text-sm opacity-90 mb-6">par <%= plan[:interval] %></div>
            </div>

            <ul class="space-y-2 mb-6">
              <% plan[:features].take(5).each do |feature| %>
                <li class="flex items-start gap-2 text-sm">
                  <svg class="w-4 h-4 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span><%= feature %></span>
                </li>
              <% end %>
            </ul>

            <% if is_current %>
              <div class="text-center px-4 py-2 bg-white/20 rounded-lg font-medium">
                Forfait actuel
              </div>
            <% else %>
              <%= button_to buyer_subscription_path, method: :patch, params: { plan_type: "buyer_#{plan_key}" }, class: "w-full px-4 py-2 bg-white text-buyer-600 rounded-lg font-medium hover:bg-buyer-50 transition", data: { turbo_confirm: "Passer au forfait #{plan[:name]} ?" } do %>
                Passer à <%= plan[:name] %>
              <% end %>
            <% end %>
          </div>
        <% elsif plan_key == :club %>
          <!-- Club Plan (Highlighted) -->
          <div class="relative bg-white rounded-xl shadow-lg p-6 border-2 border-purple-300">
            <% if is_current %>
              <div class="absolute top-4 right-4 bg-purple-600 text-white px-3 py-1 text-xs font-bold uppercase rounded-full">
                Actuel
              </div>
            <% else %>
              <div class="absolute top-4 right-4">
                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-purple-100 text-purple-900">
                  💎 Exclusif
                </span>
              </div>
            <% end %>

            <div class="mb-6 <%= is_current ? '' : 'mt-8' %>">
              <h3 class="text-2xl font-bold text-gray-900 mb-2"><%= plan[:name] %></h3>
              <div class="text-3xl font-bold text-gray-900 mb-1"><%= plan[:price_display] %></div>
              <div class="text-sm text-gray-500 mb-6">par <%= plan[:interval] %></div>
            </div>

            <ul class="space-y-2 mb-6">
              <% plan[:features].take(5).each do |feature| %>
                <li class="flex items-start gap-2 text-sm">
                  <svg class="w-4 h-4 text-purple-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span class="text-gray-700"><%= feature %></span>
                </li>
              <% end %>
            </ul>

            <% if is_current %>
              <div class="text-center px-4 py-2 bg-gray-100 text-gray-600 rounded-lg font-medium">
                Forfait actuel
              </div>
            <% else %>
              <%= button_to buyer_subscription_path, method: :patch, params: { plan_type: "buyer_#{plan_key}" }, class: "w-full px-4 py-2 bg-purple-600 text-white rounded-lg font-medium hover:bg-purple-700 transition", data: { turbo_confirm: "Passer au forfait #{plan[:name]} ?" } do %>
                Passer à <%= plan[:name] %>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <!-- Standard Plans (Free, Starter) -->
          <div class="relative bg-white rounded-xl shadow-lg p-6 border-2 <%= is_current ? 'border-buyer-500' : 'border-gray-200' %>">
            <% if is_current %>
              <div class="absolute top-4 right-4 bg-buyer-500 text-white px-3 py-1 text-xs font-bold uppercase rounded-full">
                Actuel
              </div>
            <% end %>

            <div class="mb-6">
              <h3 class="text-2xl font-bold text-gray-900 mb-2"><%= plan[:name] %></h3>
              <div class="text-3xl font-bold text-buyer-600 mb-1"><%= plan[:price_display] %></div>
              <div class="text-sm text-gray-500 mb-6">par <%= plan[:interval] %></div>
            </div>

            <ul class="space-y-2 mb-6">
              <% plan[:features].take(5).each do |feature| %>
                <li class="flex items-start gap-2 text-sm">
                  <svg class="w-4 h-4 text-green-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span class="text-gray-700"><%= feature %></span>
                </li>
              <% end %>
            </ul>

            <% if is_current %>
              <div class="text-center px-4 py-2 bg-gray-100 text-gray-600 rounded-lg font-medium">
                Forfait actuel
              </div>
            <% elsif plan_key == :free %>
              <button class="w-full px-4 py-2 border-2 border-gray-300 text-gray-500 rounded-lg font-medium cursor-not-allowed" disabled>
                Rétrogradation non disponible
              </button>
            <% else %>
              <%= button_to buyer_subscription_path, method: :patch, params: { plan_type: "buyer_#{plan_key}" }, class: "w-full px-4 py-2 bg-buyer-600 text-white rounded-lg font-medium hover:bg-buyer-700 transition", data: { turbo_confirm: "Passer au forfait #{plan[:name]} ?" } do %>
                Passer à <%= plan[:name] %>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- What credits do -->
    <div class="bg-blue-50 rounded-xl p-6">
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0">
          <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div>
          <h3 class="font-bold text-gray-900 mb-2">À quoi servent les crédits ?</h3>
          <p class="text-sm text-gray-700 mb-3">Les crédits vous donnent accès aux services premium :</p>
          <ul class="space-y-2 text-sm text-gray-700">
            <li class="flex items-start gap-2">
              <span class="text-blue-600">•</span>
              <span><strong>Enrichissement annonce</strong> - 2 crédits : Demander des informations financières détaillées</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-blue-600">•</span>
              <span><strong>Contact partenaire</strong> - 1 crédit : Obtenir les coordonnées d'un expert (avocat, comptable, etc.)</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-blue-600">•</span>
              <span><strong>Rapport d'évaluation</strong> - 5 crédits : Analyse approfondie d'une opportunité</span>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <!-- Credit Packs -->
    <div class="bg-white rounded-xl shadow-sm p-8 border border-gray-200">
      <div class="text-center mb-8">
        <h2 class="text-2xl font-bold text-gray-900 mb-2">Besoin de plus de crédits ?</h2>
        <p class="text-gray-600">Achetez des packs de crédits à tout moment</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="border-2 border-gray-200 rounded-xl p-6 hover:border-buyer-500 transition text-center">
          <div class="text-xl font-bold text-gray-900 mb-2">Pack Starter</div>
          <div class="text-3xl font-bold text-buyer-600 mb-1">10 crédits</div>
          <div class="text-2xl font-bold text-gray-900 mb-4">50€</div>
          <p class="text-sm text-gray-600 mb-4">5€ par crédit</p>
          <%= link_to "Acheter", buyer_credits_path, class: "inline-block w-full px-4 py-3 bg-buyer-600 text-white rounded-lg hover:bg-buyer-700 transition font-medium text-center" %>
        </div>

        <div class="border-2 border-buyer-500 rounded-xl p-6 relative text-center bg-buyer-50">
          <div class="absolute -top-3 left-1/2 transform -translate-x-1/2">
            <span class="px-3 py-1 bg-buyer-600 text-white text-xs font-medium rounded-full">⭐ Populaire</span>
          </div>
          <div class="text-xl font-bold text-gray-900 mb-2">Pack Pro</div>
          <div class="text-3xl font-bold text-buyer-600 mb-1">25 crédits</div>
          <div class="text-2xl font-bold text-gray-900 mb-4">100€</div>
          <p class="text-sm text-gray-600 mb-4">4€ par crédit <span class="text-green-600 font-medium">(-20%)</span></p>
          <%= link_to "Acheter", buyer_credits_path, class: "inline-block w-full px-4 py-3 bg-buyer-600 text-white rounded-lg hover:bg-buyer-700 transition font-medium text-center" %>
        </div>

        <div class="border-2 border-gray-200 rounded-xl p-6 hover:border-buyer-500 transition text-center">
          <div class="inline-block px-2 py-1 bg-green-100 text-green-800 text-xs font-medium rounded mb-2">
            Meilleur rapport
          </div>
          <div class="text-xl font-bold text-gray-900 mb-2">Pack Expert</div>
          <div class="text-3xl font-bold text-buyer-600 mb-1">50 crédits</div>
          <div class="text-2xl font-bold text-gray-900 mb-4">180€</div>
          <p class="text-sm text-gray-600 mb-4">3,60€ par crédit <span class="text-green-600 font-medium">(-28%)</span></p>
          <%= link_to "Acheter", buyer_credits_path, class: "inline-block w-full px-4 py-3 bg-buyer-600 text-white rounded-lg hover:bg-buyer-700 transition font-medium text-center" %>
        </div>
      </div>

      <div class="mt-6 text-center text-sm text-gray-600">
        <p>💎 <strong>Membres Club :</strong> Bénéficiez de 20% de réduction supplémentaire sur tous les packs !</p>
      </div>
    </div>

    <!-- Info Box -->
    <div class="bg-gray-50 rounded-xl p-6">
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0">
          <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div>
          <h3 class="font-bold text-gray-900 mb-2">À propos des changements de forfait</h3>
          <ul class="space-y-2 text-sm text-gray-700">
            <li class="flex items-start gap-2">
              <span class="text-gray-600">•</span>
              <span><strong>Les améliorations</strong> prennent effet immédiatement et vous serez facturé au prorata de la différence</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-gray-600">•</span>
              <span><strong>Les rétrogradations</strong> prennent effet à la fin de votre période de facturation actuelle</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-gray-600">•</span>
              <span>Vos crédits achetés restent valables lors d'un changement de forfait</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-gray-600">•</span>
              <span>Les crédits mensuels inclus sont ajustés selon votre nouveau forfait</span>
            </li>
          </ul>
        </div>
      </div>
    </div>

  <% else %>
    <div class="text-center py-12">
      <p class="text-gray-500 mb-4">Aucun abonnement actif à modifier</p>
      <%= link_to "Voir les forfaits d'abonnement", new_buyer_subscription_path, class: "inline-flex items-center gap-2 px-6 py-3 bg-buyer-600 text-white rounded-lg font-medium hover:bg-buyer-700 transition" %>
    </div>
  <% end %>
</div>

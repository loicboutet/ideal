<% content_for :title, "Tarifs - Idéal Reprise" %>

<!-- Hero Section -->
<section class="bg-gradient-to-br from-blue-600 to-blue-800 text-white py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center max-w-3xl mx-auto">
      <h1 class="text-4xl lg:text-6xl font-bold mb-6">
        <% if @user_role == 'seller' %>
          Tarif Vendeur
        <% elsif @user_role == 'partner' %>
          Tarif Partenaire
        <% else %>
          Tarifs et forfaits
        <% end %>
      </h1>
      <p class="text-xl lg:text-2xl text-blue-100">
        <% if @user_role == 'seller' %>
          Maximisez la visibilité de vos annonces avec notre forfait premium
        <% elsif @user_role == 'partner' %>
          Rejoignez notre réseau de partenaires experts
        <% else %>
          Choisissez l'offre adaptée à votre projet de reprise d'entreprise
        <% end %>
      </p>
    </div>
  </div>
</section>

<!-- Subscription Plans -->
<section class="py-20 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
        <% if @user_role == 'seller' %>
          Forfait Premium Vendeur
        <% elsif @user_role == 'partner' %>
          Forfait Partenaire
        <% else %>
          Forfaits d'abonnement
        <% end %>
      </h2>
      <p class="text-xl text-gray-600">
        <% if @user_role == 'seller' %>
          Maximisez vos opportunités avec un accès illimité
        <% elsif @user_role == 'partner' %>
          Augmentez votre visibilité auprès de nos utilisateurs
        <% else %>
          Des forfaits flexibles pour tous les repreneurs
        <% end %>
      </p>
    </div>

    <!-- Plan Comparison -->
    <% if @user_role == 'seller' || @user_role == 'partner' %>
      <!-- Single plan display for sellers and partners -->
      <div class="max-w-4xl mx-auto">
        <% @all_plans.reject { |key, _| key == :free }.each do |plan_key, plan| %>
          <div class="relative bg-gradient-to-br from-blue-500 to-purple-600 rounded-xl shadow-xl p-10 text-white">
            <div class="absolute top-6 right-6">
              <span class="inline-flex items-center px-4 py-2 rounded-full text-sm font-medium bg-yellow-400 text-gray-900">
                ⭐ Recommandé
              </span>
            </div>

            <div class="mb-8 mt-8">
              <h3 class="text-3xl font-bold mb-3"><%= plan[:name] %></h3>
              <div class="text-5xl font-bold mb-2"><%= plan[:price_display] %></div>
              <div class="text-lg opacity-90 mb-8">par <%= plan[:interval] == 'month' ? 'mois' : 'an' %></div>
            </div>

            <ul class="space-y-3 mb-8">
              <% plan[:features].each do |feature| %>
                <li class="flex items-start gap-3 text-base">
                  <svg class="w-5 h-5 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span><%= feature %></span>
                </li>
              <% end %>
            </ul>

            <% if user_signed_in? %>
              <% subscription_path = case current_user.role
                when 'seller' then seller_subscription_path
                when 'partner' then partner_subscription_path
                else new_user_session_path
              end %>
              <%= link_to subscription_path, class: "block w-full px-6 py-3 bg-white text-blue-600 rounded-lg font-semibold hover:bg-blue-50 transition text-center text-lg" do %>
                Choisir <%= plan[:name] %>
              <% end %>
            <% else %>
              <%= link_to new_user_session_path, class: "block w-full px-6 py-3 bg-white text-blue-600 rounded-lg font-semibold hover:bg-blue-50 transition text-center text-lg" do %>
                Choisir <%= plan[:name] %>
              <% end %>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <!-- Multiple plans display for buyers -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      <% @all_plans.reject { |key, _| key == :free }.each do |plan_key, plan| %>
        <% if plan_key == :premium %>
          <!-- Premium Plan (Highlighted) -->
          <div class="relative bg-gradient-to-br from-blue-500 to-purple-600 rounded-xl shadow-xl p-6 text-white border-2 border-blue-600">
            <div class="absolute top-4 right-4">
              <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-yellow-400 text-gray-900">
                ⭐ Recommandé
              </span>
            </div>

            <div class="mb-6 mt-8">
              <h3 class="text-2xl font-bold mb-2"><%= plan[:name] %></h3>
              <div class="text-3xl font-bold mb-1"><%= plan[:price_display] %></div>
              <div class="text-sm opacity-90 mb-6">par <%= plan[:interval] == 'month' ? 'mois' : 'an' %></div>
            </div>

            <ul class="space-y-2 mb-6">
              <% plan[:features].take(5).each do |feature| %>
                <li class="flex items-start gap-2 text-sm">
                  <svg class="w-4 h-4 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span><%= feature %></span>
                </li>
              <% end %>
            </ul>

            <% if user_signed_in? %>
              <% subscription_path = case current_user.role
                when 'buyer' then edit_buyer_subscription_path
                when 'seller' then seller_subscription_path
                when 'partner' then partner_subscription_path
                else new_user_session_path
              end %>
              <%= link_to subscription_path, class: "block w-full px-4 py-2 bg-white text-blue-600 rounded-lg font-medium hover:bg-blue-50 transition text-center" do %>
                Choisir <%= plan[:name] %>
              <% end %>
            <% else %>
              <%= link_to new_user_session_path, class: "block w-full px-4 py-2 bg-white text-blue-600 rounded-lg font-medium hover:bg-blue-50 transition text-center" do %>
                Choisir <%= plan[:name] %>
              <% end %>
            <% end %>
          </div>
        <% elsif plan_key == :club %>
          <!-- Club Plan (Highlighted) -->
          <div class="relative bg-white rounded-xl shadow-lg p-6 border-2 border-purple-300">
            <div class="absolute top-4 right-4">
              <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-purple-100 text-purple-900">
                💎 Exclusif
              </span>
            </div>

            <div class="mb-6 mt-8">
              <h3 class="text-2xl font-bold text-gray-900 mb-2"><%= plan[:name] %></h3>
              <div class="text-3xl font-bold text-gray-900 mb-1"><%= plan[:price_display] %></div>
              <div class="text-sm text-gray-500 mb-6">par <%= plan[:interval] == 'month' ? 'mois' : 'an' %></div>
            </div>

            <ul class="space-y-2 mb-6">
              <% plan[:features].take(5).each do |feature| %>
                <li class="flex items-start gap-2 text-sm">
                  <svg class="w-4 h-4 text-purple-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span class="text-gray-700"><%= feature %></span>
                </li>
              <% end %>
            </ul>

            <% if user_signed_in? %>
              <% subscription_path = case current_user.role
                when 'buyer' then edit_buyer_subscription_path
                when 'seller' then seller_subscription_path
                when 'partner' then partner_subscription_path
                else new_user_session_path
              end %>
              <%= link_to subscription_path, class: "block w-full px-4 py-2 bg-purple-600 text-white rounded-lg font-medium hover:bg-purple-700 transition text-center" do %>
                Choisir <%= plan[:name] %>
              <% end %>
            <% else %>
              <%= link_to new_user_session_path, class: "block w-full px-4 py-2 bg-purple-600 text-white rounded-lg font-medium hover:bg-purple-700 transition text-center" do %>
                Choisir <%= plan[:name] %>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <!-- Standard Plans (Free, Starter, Standard) -->
          <div class="relative bg-white rounded-xl shadow-lg p-6 border-2 border-gray-200">
            <div class="mb-6">
              <h3 class="text-2xl font-bold text-gray-900 mb-2"><%= plan[:name] %></h3>
              <div class="text-3xl font-bold text-blue-600 mb-1"><%= plan[:price_display] %></div>
              <div class="text-sm text-gray-500 mb-6">par <%= plan[:interval] == 'month' ? 'mois' : 'an' %></div>
            </div>

            <ul class="space-y-2 mb-6">
              <% plan[:features].take(5).each do |feature| %>
                <li class="flex items-start gap-2 text-sm">
                  <svg class="w-4 h-4 text-green-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span class="text-gray-700"><%= feature %></span>
                </li>
              <% end %>
            </ul>

            <% if plan_key == :free %>
              <% if user_signed_in? %>
                <%= link_to edit_buyer_subscription_path, class: "block w-full px-4 py-2 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition text-center" do %>
                  Commencer gratuitement
                <% end %>
              <% else %>
                <%= link_to new_user_registration_path, class: "block w-full px-4 py-2 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition text-center" do %>
                  Commencer gratuitement
                <% end %>
              <% end %>
            <% else %>
              <% if user_signed_in? %>
                <% subscription_path = case current_user.role
                  when 'buyer' then edit_buyer_subscription_path
                  when 'seller' then seller_subscription_path
                  when 'partner' then partner_subscription_path
                  else new_user_session_path
                end %>
                <%= link_to subscription_path, class: "block w-full px-4 py-2 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition text-center" do %>
                  Choisir <%= plan[:name] %>
                <% end %>
              <% else %>
                <%= link_to new_user_session_path, class: "block w-full px-4 py-2 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition text-center" do %>
                  Choisir <%= plan[:name] %>
                <% end %>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
      </div>
    <% end %>

    <!-- What credits do (only for buyers) -->
    <% if @user_role == 'buyer' %>
    <div class="mt-12 bg-blue-50 rounded-xl p-6">
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0">
          <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <div>
          <h3 class="font-bold text-gray-900 mb-2">À quoi servent les crédits ?</h3>
          <p class="text-sm text-gray-700 mb-3">Les crédits vous donnent accès aux services premium :</p>
          <ul class="space-y-2 text-sm text-gray-700">
            <li class="flex items-start gap-2">
              <span class="text-blue-600">•</span>
              <span><strong>Enrichissement annonce</strong> - 2 crédits : Demander des informations financières détaillées</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-blue-600">•</span>
              <span><strong>Contact partenaire</strong> - 1 crédit : Obtenir les coordonnées d'un expert (avocat, comptable, etc.)</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-blue-600">•</span>
              <span><strong>Rapport d'évaluation</strong> - 5 crédits : Analyse approfondie d'une opportunité</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</section>

<!-- Credit Packs (only for buyers) -->
<% if @user_role == 'buyer' %>
<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
        Packs de crédits
      </h2>
      <p class="text-xl text-gray-600">
        Achetez des crédits supplémentaires à tout moment
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
      <div class="border-2 border-gray-200 rounded-xl p-8 hover:border-blue-500 hover:shadow-lg transition text-center">
        <div class="text-xl font-bold text-gray-900 mb-2">Pack Starter</div>
        <div class="text-3xl font-bold text-blue-600 mb-1">10 crédits</div>
        <div class="text-2xl font-bold text-gray-900 mb-4">50€</div>
        <p class="text-sm text-gray-600 mb-6">5€ par crédit</p>
        <% if user_signed_in? %>
          <%= link_to buyer_credits_path, class: "block w-full px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium text-center" do %>
            Acheter
          <% end %>
        <% else %>
          <%= link_to new_user_session_path, class: "block w-full px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium text-center" do %>
            Acheter
          <% end %>
        <% end %>
      </div>

      <div class="border-2 border-blue-500 rounded-xl p-8 relative bg-blue-50 shadow-lg">
        <div class="absolute -top-3 left-1/2 transform -translate-x-1/2">
          <span class="px-3 py-1 bg-blue-600 text-white text-xs font-medium rounded-full">⭐ Populaire</span>
        </div>
        <div class="text-center">
          <div class="text-xl font-bold text-gray-900 mb-2">Pack Pro</div>
          <div class="text-3xl font-bold text-blue-600 mb-1">25 crédits</div>
          <div class="text-2xl font-bold text-gray-900 mb-4">100€</div>
          <p class="text-sm text-gray-600 mb-6">4€ par crédit <span class="text-green-600 font-medium">(-20%)</span></p>
          <% if user_signed_in? %>
            <%= link_to buyer_credits_path, class: "block w-full px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium text-center" do %>
              Acheter
            <% end %>
          <% else %>
            <%= link_to new_user_session_path, class: "block w-full px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium text-center" do %>
              Acheter
            <% end %>
          <% end %>
        </div>
      </div>

      <div class="border-2 border-gray-200 rounded-xl p-8 hover:border-blue-500 hover:shadow-lg transition text-center">
        <div class="inline-block px-2 py-1 bg-green-100 text-green-800 text-xs font-medium rounded mb-2">
          Meilleur rapport
        </div>
        <div class="text-xl font-bold text-gray-900 mb-2">Pack Expert</div>
        <div class="text-3xl font-bold text-blue-600 mb-1">50 crédits</div>
        <div class="text-2xl font-bold text-gray-900 mb-4">180€</div>
        <p class="text-sm text-gray-600 mb-6">3,60€ par crédit <span class="text-green-600 font-medium">(-28%)</span></p>
        <% if user_signed_in? %>
          <%= link_to buyer_credits_path, class: "block w-full px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium text-center" do %>
            Acheter
          <% end %>
        <% else %>
          <%= link_to new_user_session_path, class: "block w-full px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium text-center" do %>
            Acheter
          <% end %>
        <% end %>
      </div>
    </div>

    <div class="mt-8 text-center text-sm text-gray-600">
      <p>💎 <strong>Membres Club :</strong> Bénéficiez de 20% de réduction supplémentaire sur tous les packs !</p>
    </div>
  </div>
</section>
<% end %>

<!-- FAQ Section -->
<section class="py-20 bg-gray-50">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
        Questions fréquentes
      </h2>
    </div>

    <div class="space-y-6">
      <div class="bg-white rounded-lg p-6 shadow-md">
        <h3 class="text-lg font-bold text-gray-900 mb-2">Puis-je changer de forfait à tout moment ?</h3>
        <p class="text-gray-600">
          Oui, vous pouvez changer de forfait à tout moment. Les améliorations prennent effet immédiatement, 
          tandis que les rétrogradations prennent effet à la fin de votre période de facturation.
        </p>
      </div>

      <div class="bg-white rounded-lg p-6 shadow-md">
        <h3 class="text-lg font-bold text-gray-900 mb-2">Que se passe-t-il si je n'utilise pas tous mes crédits ?</h3>
        <p class="text-gray-600">
          Les crédits inclus dans votre abonnement sont renouvelés chaque mois. Les crédits achetés séparément 
          n'expirent jamais et restent disponibles dans votre compte.
        </p>
      </div>

      <div class="bg-white rounded-lg p-6 shadow-md">
        <h3 class="text-lg font-bold text-gray-900 mb-2">Puis-je annuler mon abonnement ?</h3>
        <p class="text-gray-600">
          Oui, vous pouvez annuler votre abonnement à tout moment depuis votre espace personnel. 
          Vous conserverez l'accès à votre forfait jusqu'à la fin de la période payée.
        </p>
      </div>

      <div class="bg-white rounded-lg p-6 shadow-md">
        <h3 class="text-lg font-bold text-gray-900 mb-2">Y a-t-il des frais cachés ?</h3>
        <p class="text-gray-600">
          Non, tous nos tarifs sont transparents. Le seul coût supplémentaire correspond aux packs de crédits 
          que vous choisissez d'acheter pour accéder aux services premium.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="py-20 bg-white">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-6">
      <% if @user_role == 'seller' %>
        Prêt à maximiser vos opportunités ?
      <% elsif @user_role == 'partner' %>
        Rejoignez notre réseau de partenaires
      <% else %>
        Prêt à trouver votre entreprise idéale ?
      <% end %>
    </h2>
    <p class="text-xl text-gray-600 mb-8">
      <% if @user_role == 'seller' %>
        Commencez dès aujourd'hui et donnez une visibilité maximale à vos annonces
      <% elsif @user_role == 'partner' %>
        Développez votre activité en rejoignant notre plateforme
      <% else %>
        Commencez gratuitement et passez à un forfait payant quand vous le souhaitez
      <% end %>
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <% if user_signed_in? %>
        <% subscription_path = case current_user.role
          when 'buyer' then edit_buyer_subscription_path
          when 'seller' then seller_subscription_path
          when 'partner' then partner_subscription_path
          else new_user_session_path
        end %>
        <%= link_to "Voir mon abonnement", subscription_path, class: "bg-blue-600 text-white px-8 py-4 rounded-lg font-semibold hover:bg-blue-700 transition-colors text-lg" %>
      <% else %>
        <%= link_to "Créer mon compte gratuitement", new_user_registration_path, class: "bg-blue-600 text-white px-8 py-4 rounded-lg font-semibold hover:bg-blue-700 transition-colors text-lg" %>
      <% end %>
      <%= link_to "Nous contacter", contact_path, class: "bg-gray-100 text-gray-900 px-8 py-4 rounded-lg font-semibold hover:bg-gray-200 transition-colors text-lg" %>
    </div>
  </div>
</section>
